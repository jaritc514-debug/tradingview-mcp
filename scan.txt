SCAN — HTF Supply & Demand
=====================================
You are scanning the pair currently open on TradingView for HTF Supply & Demand zones. Weekly zones can be
Fresh or Touched (confluence only). Daily zones are Fresh-only — the user does not enter on Touched Daily
zones, so those aren't pulled into scope at all. Quick scan only — no manual fib math. See rules.json.

Run about twice a week (before market open, and midweek) — scope is intentionally bounded per
rules.json zone_detection.scan_scope: Weekly nearest 2 Supply + 2 Demand (any freshness — confluence only).
Daily nearest 3 FRESH Supply + 3 FRESH Demand — Touched Daily zones are not entry candidates, skip them.

---

STEP 1 — PULL THE IN-SCOPE ZONES (Weekly + Daily)
- chart_set_timeframe → Weekly
- data_get_study_values() → read "Supply N Proximal/Distal" and "Demand N Proximal/Distal" for the
  Supply & Demand Pro study. This list is ordered nearest-to-farthest from current price. Keep Supply 1-2
  and Demand 1-2 only, regardless of freshness (fewer if fewer exist — don't pad).
- data_get_pine_boxes(study_filter="Supply & Demand Pro", verbose=true) and
  data_get_pine_labels(study_filter="Supply & Demand Pro") → match by price range to the zones just kept,
  to read formation (RBD/RBR/DBR/DBD), F (flip), O (original), LoL (level on level), and freshness color
  (see rules.json indicator_tags.freshness_detection: #1c1c2e = Fresh, #787b86 = Touched, null = inactive —
  ignore null zones, they shouldn't appear in the study_values list anyway).
- chart_set_timeframe → Daily
- data_get_study_values() again — this can return more than 3 per side (e.g. Supply 1-5). Match ALL of
  them against pine_boxes/pine_labels for freshness, then keep only the nearest 3 FRESH Supply and nearest
  3 FRESH Demand — skip Touched ones and keep checking further down the list until 3 Fresh are found (or
  fewer, if fewer than 3 Fresh exist across the whole active list — don't fall back to Touched, don't pad).
- If the indicator returns no data at all: flag this pair as UNREADABLE and stop here.

STEP 2 — HTF COVERAGE
For each in-scope Daily zone, check whether it sits inside or overlaps an in-scope Weekly zone of the
same type (both supply or both demand). HTF Coverage = Yes / No. Not mandatory — report both either way.

STEP 3 — LEG-OUT QUALITY — DAILY ZONES ONLY
Weekly zones never get a risk tier or Entry/SL/TP, so skip this step entirely for them — don't fetch
Weekly OHLCV or classify Weekly breaking candles at all, this was wasted work.
The indicator's own base-length filter is already capped at 6 candles — do not re-validate base length.
For each in-scope DAILY zone, pull candles via data_get_ohlcv around its formation and classify the
candle that broke the base:
  ≤49% body = indecisive | 50-75% = decisive | >75% (and abnormally large) = explosive
Leg-out quality = Explosive / Decisive. If the OHLCV window fetched doesn't reach back to the zone's
formation (older zone, small pair, whatever), report "not computed — formation predates fetched history"
rather than guessing — don't force a classification. If the breaking candle reads as indecisive, flag as
unusual — the indicator likely wouldn't have drawn this zone.
Also check the candle immediately AFTER the breaking candle and classify it too. strong_leg_out = TRUE if
the breaking candle is Explosive, OR if the breaking candle AND the next candle are BOTH Decisive — this
stricter check only matters for the 2% risk tier (Step 7), not the normal leg-out quality reported above.

STEP 4 — TREND CONFLUENCE (tag only)
- data_get_pine_tables(study_filter="Trend Direction Pro") → read Daily and Weekly rows
- Record Trend (Bullish/Bearish), Status (Confirmed/Unconfirmed), Strength (Trending/Ranging) for both
- Tag each zone as trend-aligned or trend-conflicting. NEVER exclude a zone based on this.

STEP 5 — VALUATION CONFLUENCE (tag only — pick the right indicator per asset, see rules.json)
- Daily timeframe ONLY — never read valuation on Weekly. chart_set_timeframe should already be Daily from
  Step 1, but confirm before this read. One read for the whole symbol.
- DAILY ZONE OUTPUT ONLY. It's a flat per-symbol reading, identical no matter which zone you're looking
  at, so don't attach it to Weekly zone blocks at all — that's misleading, not a real per-zone signal.
  Trend still appears on Weekly (it's a real input to the 2% tier), valuation does not.
- Classify the pair first, then use the matching study_filter — do not try all three:
    Currency future / FX major / commodity / metal → study_filter="CampusValuationTool" (exact, no spaces)
    Index future (NQ/YM/ES/RTY)                     → study_filter="Supreme Valuation"
    FX cross pair (non-USD, e.g. EURGBP, GBPJPY)     → study_filter="MTF Cross Pairs Valuation"
- data_get_pine_tables(study_filter=<the one selected above>) — if this fails, try data_get_study_values
  with the same study_filter
- Prior UNREADABLE result was likely just the wrong name ("Campus Valuation Tool" with spaces doesn't
  match "CampusValuationTool"). If the corrected name still fails, the indicator's bottom pane may need
  to be actively visible/expanded before TradingView renders its data for CDP reads — try chart_get_state
  or ui_click on the pane's legend before retrying.
- Convert the raw numeric value to a state per rules.json valuation_confluence.classification:
    >= 75 Extreme Overvalued | 0.5 to 74.99 Overvalued | -0.5 to 0.5 Neutral
    -74.99 to -0.5 Undervalued | <= -75 Extreme Undervalued
- Tag only. NEVER exclude a zone based on this, even once readable.

STEP 6 — PROFIT CHECK (2R-based, per rules.json profit_check) — DAILY ZONES ONLY
Weekly is never entered directly — it's an odds-enhancer for HTF Coverage only. Skip this step entirely
for Weekly zones, no Entry/SL/TP line for them.
- Entry = zone's proximal edge (the edge nearest current price)
- SL = distal edge, offset by 33% of the zone's own height, away from the zone:
    Demand (long):  SL = distal − 0.33 × (proximal − distal)
    Supply (short): SL = distal + 0.33 × (distal − proximal)
- R = |entry − SL|. TP = entry ± 2R (2R is fixed, not zone-derived).
- Check whether any opposing-type in-scope Daily zone sits between entry and the 2R target.
- Path = Clear (nothing blocks the 2R target) / Boxed in (an opposing zone sits inside that range — name it
  and its price).

STEP 7 — RISK TIER — DAILY ZONES ONLY
Check top-down against rules.json risk_tiers — a zone gets the highest tier whose conditions it fully meets:
  2% (All Stars Aligned): HTF Coverage Yes AND strong_leg_out TRUE AND Weekly trend aligned with direction
    (Daily aligned too is a bonus, not required) AND Extreme Undervalued (Demand/buy) or Extreme
    Overvalued (Supply/sell). ALL FOUR required — if any one fails, check the tiers below instead.
  1.5%: Explosive or Decisive leg-out, HTF Coverage present
  1%: Decisive+ leg-out, no HTF Coverage
  0.5%: everything else that still clears the basic checklist
No tier for Weekly zones — see risk_tiers.scope_note.

STEP 8 — PROXIMITY CHECK (drives the Action Summary only — doesn't affect scope, tiers, or the main zone
list above, which always reports every in-scope zone regardless of distance)
Pull ~14 recent Daily candles via data_get_ohlcv and compute a simple ATR (average high-low range — see
rules.json proximity_check.atr_method, this is a rough gauge, not a precision indicator). A zone is
"actionable now" if current price is within roughly 1-2 ATR of its proximal (near) edge. Applies to both
Weekly and Daily in-scope zones — Weekly can be "approaching" too, it just still never gets Entry/SL/TP.

---

OUTPUT — one header per pair, then one block per in-scope zone (Weekly and Daily reported separately,
nearest-to-price first). Trend and Valuation are flat per-symbol facts — state them ONCE in the header,
not repeated in every zone block. Each zone only shows how it relates to those facts (Trend fit), not the
raw readings again.

=== [PAIR] — HTF Supply & Demand Scan (last price: [price]) ===
Trend: Daily [Bullish/Bearish, Confirmed/Unconfirmed, Trending/Ranging] | Weekly [same]
Valuation (Daily zones only, doesn't apply to Weekly): [Extreme Overvalued / Overvalued / Neutral /
Undervalued / Extreme Undervalued] ([raw value]) — [indicator used] — or "unreadable" if the read failed

ZONE: [Weekly/Daily] [Supply/Demand] @ [price range] — [RBD/RBR/DBR/DBD]
Freshness: [Fresh/Touched]  |  Leg-Out: [Explosive/Decisive/not computed, or "—" for Weekly zones — not computed at all, see Step 3]  |  HTF Coverage: [Yes/No for Daily zones; "n/a (Weekly)" for Weekly zones — Weekly zones are the source of HTF Coverage, not the receiver, see Step 2]
Tags: [F if flip]  [O if original]  [LoL if level-on-level]
Trend fit: [Aligned/Conflicting] (against the header's trend reading — don't restate the full reading here)

Daily zones only — omit these three lines entirely for Weekly zones:
Entry/SL/TP: [entry] / [SL] / [TP at 2R]
Path: [Clear / Boxed in — detail]
SUGGESTED RISK TIER: [2% / 1.5% / 1% / 0.5%]

If no active zones on either timeframe: "No active zones on [pair] — Weekly or Daily."

NOTES — include whenever there's something worth flagging (a capped leg-out, an indecisive/unusual breaking
candle, why nothing reached 2%, an UNREADABLE indicator, etc.); omit only if the scan came back clean with
nothing to explain. Short bullet points, not full sentences per zone.

── ACTION SUMMARY ──────────────────────────────────────────────
Direct answer, not a re-list of the technical detail above: is anything on [PAIR] worth acting on before the
next scan (weekend / Wednesday)? Per rules.json proximity_check, a zone counts as "actionable now" if current
price is within roughly 1-2 ATR (computed from the last ~14 Daily candles) of the zone's near edge. Zones
farther out are already fully reported above — don't repeat them here, they're not due for attention yet.

If one or more zones are within range, closest first:
[Weekly/Daily] [Supply/Demand] @ [range] — [~X.X ATR away] — [Risk Tier, or "confluence only, no tier" for
Weekly] — [what to do: e.g. "Watch for entry near 0.71313, SL 0.71551, TP 0.70837" for Daily; "No direct
entry — Weekly confluence zone, watch how Daily reacts here" for Weekly]

If nothing is within range: "Nothing actionable on [PAIR] right now — nearest zone is ~X ATR away. Recheck
at the next scan."
